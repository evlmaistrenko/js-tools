import { Typography } from "antd"

export const header = {
	children: (
		<div style={{ textAlign: "center", flex: 1 }}>
			<Typography.Title
				level={5}
				style={{ marginBottom: 0 }}
			>
				Header
			</Typography.Title>
		</div>
	),
}
